[package]
name = "rf-core"
version = "0.1.0"
edition = "2021"
description = "Returned Fire - deterministic simulation core (compiled to wasm)"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2.128"

[dev-dependencies]

[features]
default = []
